# Runtime dependencies for the game itself (console and web front-ends).
#
# pygame is deliberately NOT listed: it is imported lazily by the pygame
# front-end only, so the console/web front-ends run without it. Install it
# separately if you want UIType.PYGAME.
#
# tak is the text-adventure kit extracted from this game
# (https://github.com/Stephenson-Software/tak); FishE is being moved back onto
# it one module at a time. Installing it needs git on the PATH.
tak @ git+https://github.com/Stephenson-Software/tak@v0.1.0
jsonschema>=4.0,<5.0
